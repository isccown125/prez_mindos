import Markdown from "reveal.js/plugin/markdown/markdown";

export const revealConfig = {
  controls: true,
  keyboard: true,
  center: false,
  history: true,
  fragments: false,
  plugins: [Markdown],
};
